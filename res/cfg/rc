bind Q exit
bind Escape exit

bind R console_open

bind W move_cam f
bind A move_cam l
bind S move_cam b
bind D move_cam r
bind Left_Shift cam_accelerate

bind Z vis_occluders
bind X vis_worldwireframe

windowsize 800 600
map map1
