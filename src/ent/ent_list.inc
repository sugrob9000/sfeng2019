/*
 * Keyvals:
 *   model - path to model
 *   material - path to material
 * Signals:
 *   setpos - set new xyz
 *   showpos - show current xyz
 */
ENTITY (prop)

/*
 * Keyvals:
 *   time - value, in ticks, with which to spawn
 *   start-running - whether to start countdown on spawn
 * Signals:
 *   start
 *   stop
 *   set - set new value
 * Events:
 *   time_up
 */
ENTITY (timer)
